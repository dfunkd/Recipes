<Window x:Class="Recipes.DbPasswordSetup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Recipes"
        Title="DbPasswordSetup"
        WindowStyle="ToolWindow"
        SizeToContent="WidthAndHeight">
    <DockPanel LastChildFill="False">
        <TextBox x:Name="txtPassword" DockPanel.Dock="Top" Margin="5" />
        <Grid DockPanel.Dock="Top" Margin="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button x:Name="btnOk" Grid.Column="0" Content="Ok" IsDefault="True" Click="btnOk_Click" />
            <Button x:Name="btnCancel" Grid.Column="1" Content="Cnacel" IsCancel="True" Click="btnCancel_Click" />
        </Grid>
    </DockPanel>
</Window>
